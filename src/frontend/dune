(executable
  (name cli)
  (modules cli)
  (libraries
    containers
    cmdliner
    xword
    converter))

(executable
  (name gtkgui)
  (modules gtkgui)
  (libraries
    containers
    lablgtk3
    xword
    converter
    crosspad_model))
